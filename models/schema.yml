version: 2

sources:
  - name: gz_raw_data     # BigQuery dataset name
    tables:
      - name: raw_gz_sales_copy
      - name: raw_gz_product_copy
      - name: raw_gz_ship_copy


version: 2

sources:
  - name: raw              # 给 gz_raw_data 起别名 raw
    schema: gz_raw_data    # 实际 BigQuery 数据集名
    tables:
      - name: sales
        identifier: raw_gz_sales
      - name: product
        identifier: raw_gz_product
      - name: ship
        identifier: raw_gz_ship


version: 2

sources:
  - name: raw
    schema: gz_raw_data
    tables:
      - name: sales
        identifier: raw_gz_sales


version: 2

sources:
  - name: raw
    schema: gz_raw_data
    tables:
      - name: sales
        identifier: raw_gz_sales
      - name: product
        identifier: raw_gz_product
      - name: ship
        identifier: raw_gz_ship


